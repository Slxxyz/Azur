server:
  port: 8082
  servlet:
    contextPath: /firstSpring

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/azur_db?useSSL=false&serverTimezone=UTC
    username: root
    password: GX4J9ed8*
    driverClassName: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        naming:
          physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl

  logging:
    level:
      org.hibernate.SQL: DEBUG
      org.hibernate.type.descriptor.sql.BasicBinder: TRACE

